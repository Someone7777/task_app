import 'package:flutter/material.dart';

/// Application color constants.
class AppColors {
  /// Primary color of the app.
  static const MaterialColor primaryColor = Colors.grey;

  static const Color secondaryColor = Color.fromARGB(255, 253, 255, 205);

  static const Color appBarBackgroundColor = Color.fromARGB(255, 0, 0, 0);

  static const Color appLoadingCircleColor = Color.fromARGB(255, 72, 211, 227);
}
